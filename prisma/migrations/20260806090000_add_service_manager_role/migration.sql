-- Модуль «Станки» (ARCHITECTURE §4г, PRD §16), решения Артёма 05.08.2026.
-- Новая роль: менеджер-сервисник Максим — видит ТОЛЬКО картотеку станков.
-- Только ADD VALUE и ОТДЕЛЬНОЙ миграцией: Postgres не позволяет использовать новое значение enum
-- в той же транзакции, где оно создано (те же грабли, что с TaskStatus.IN_PROGRESS, 19.06.2026).
-- Существующие права построены белыми списками (isDispatcherRole = ADMIN|DISPATCHER), поэтому
-- добавление значения само по себе никуда новую роль не пускает.

-- AlterEnum
ALTER TYPE "Role" ADD VALUE 'SERVICE_MANAGER';
