# ROADMAP — vanmark-drive

Версия 1.0 · июнь 2026. Порядок работ для реализации в Claude Code. Один этап = одна рабочая сессия-две, одна ветка, свой критерий приёмки. Не перепрыгивать этапы; Definition of Done каждого — в CLAUDE.md.

## Фаза 1 — MVP (цель: команда работает в сервисе вместо Telegram-чата)

**Этап 0. Каркас проекта** — ~полдня
Репозиторий, Next.js (App Router, TS strict) + Prisma + PostgreSQL в docker compose для разработки, lint/typecheck/Vitest/Playwright подключены, `/api/health`, CLAUDE.md и skills скопированы в репо, docs/ заполнен этим пакетом.
✅ Приёмка: `pnpm dev` поднимает пустое приложение, все проверки зелёные, тестовый e2e проходит.

**Этап 1. Пользователи и вход** — ~полдня
Auth.js (логин+пароль, argon2id, JWT-cookie), роли ADMIN/DISPATCHER/DRIVER, сид: Артём (админ), Милена (диспетчер), водители Алексей Каширский и Алексей Писарев. Rate limit на вход. Заглушки экранов по ролям с редиректами.
✅ Приёмка: каждый из четырёх входит и попадает на свой экран; неверный пароль ×10 — блокируется; e2e на вход.

**Этап 2. Задачи: модель и работа диспетчера** — 1–2 дня
Схема Task/TaskType/TaskEvent (ARCHITECTURE §4), статусная матрица в `src/domain/`, сквозная нумерация (старт с **476** — решено Артёмом 04.06.2026, предварительно; сверить с фактическим последним номером чата перед прод-запуском, Этап 7). Экраны Милены: «Сегодня» (колонки по водителям + «Не назначено», пул «Без даты», dnd-назначение), «Все задачи» (фильтры/поиск), карточка задачи с историей, форма создания ≤30 сек. Справочник типов в админке.
✅ Приёмка: Милена создаёт задачу всех 10 типов, назначает перетаскиванием, переносит/отменяет с причиной; история пишется; unit-тесты матрицы зелёные.

**Этап 3. PWA водителя и изоляция** — 1–2 дня
Экраны водителя: «Мои задачи» (сегодня/ближайшие), карточка (Навигатор, Позвонить, индикатор пропуска), кнопка следующего статуса с гео-меткой, комментарии. Изоляция по ARCHITECTURE §6. Оптимистичный UI с ретраем.
✅ Приёмка: полный прогон security-check (все тесты изоляции зелёные); водитель проходит цепочку статусов с телефона; чужие задачи недоступны ни списком, ни по id.

**Этап 4. Фото-отчёты** — ~1 день
Загрузка фото с камеры (сжатие до ~1920px), привязка к задаче, серверное требование фото для DONE по типу, галерея в карточке у Милены, раздача файлов с проверкой прав.
✅ Приёмка: DONE без фото для «Доставки в аренду» невозможен; фото видно Милене и не видно второму водителю; security-check на эндпоинты файлов.

**Этап 5. Push и PWA-установка** — ~1 день
Web App Manifest, рукописный service worker `public/sw.js` (офлайн-режим реализован позже — этапы O1–O6 ниже, ARCHITECTURE §11), установка на домашний экран Android; web-push (VAPID): новая задача / изменение / перенос / отмена; утреннее напоминание (08:00) и предупреждение диспетчеру о незаказанных пропусках на завтра (16:00) через node-cron (старт из `instrumentation.ts`).
✅ Приёмка: на реальном Android-телефоне: PWA установлена, пуш о новой задаче приходит при закрытом приложении, тап открывает карточку.

**Этап 6. Живые обновления и полировка** — ~1 день
SWR-поллинг 10 с без дёрганья раскладки, счётчики и блок «Требуют внимания» на «Сегодня», пустые/ошибочные состояния по ui-guidelines, прогон всех экранов на 360×740.
✅ Приёмка: изменение от водителя видно у Милены ≤10 с без F5; чек-лист ui-guidelines пройден по всем экранам.

**Этап 7. Прод и запуск** — ~1 день
VPS в РФ, Docker Compose + Caddy (HTTPS), домен, бэкапы с проверкой восстановления, сид прода, прогон deploy-release целиком, smoke на проде.
✅ Приёмка: сервис доступен по https, вход с телефонов водителей, бэкап восстановлен на тестовом контейнере.

**Этап 8. Пилот 2 недели** — финал фазы 1
Все новые задачи — в сервисе (чат — только для общения). Сбор фидбека от Милены и водителей, быстрые правки. Метрики из PRD §1.
✅ Приёмка фазы 1: Артём подтверждает — чат больше не используется для постановки задач.

Оценка фазы 1: ~6–9 рабочих дней чистой работы в Claude Code.

## Фаза 1.5 — KPI и зарплата водителей (новый модуль)

Добавлен по решению Артёма (июнь 2026). Спека — PRD §12, модель данных — ARCHITECTURE §4а. Очерёдность согласована: **после Этапа 5 (пуши)**. От пилота (Этап 8) не зависит — может идти параллельно или сразу за MVP-ядром. KPI считается от ошибок (нарушений), а не от объёма работы; формула — оклад + (премия − прогрессивные штрафы) + ручные поощрения. Кандидатов в нарушения предлагает система, подтверждает Милена.

**Этап 9. KPI: модель, правила, учёт у Милены** — 1–2 дня
Аддитивная миграция (`KpiMark` / `DriverPayProfile` / `KpiRule` / `PayrollStatement` + поле `TaskType.requiresSignedDoc`); сид: `requiresSignedDoc=true` ремонтным типам, дефолтные веса/премия/оклады-заглушки и строки `KpiRule`. Доменное ядро `src/domain/kpi.ts` (детекторы нарушений + прогрессивный расчёт, юнит-тесты) и `kpi-service.ts`. Ночной cron-детектор кандидатов (идемпотентный). Экран Милены «KPI / Зарплата»: кандидаты (подтвердить/отклонить), ручная отметка/поощрение, расчёт по водителю, «Закрыть месяц». Админ-настройки оплаты (оклад/премия/веса/прогрессия). security-check на все ручки.
✅ Приёмка: система предлагает кандидатов из реальных задач (опоздание/без акта/невыполненная точка); Милена подтверждает/отклоняет одним действием; расчёт совпадает с примером PRD §12.3; закрытый месяц не меняется при правке отметок; unit-тесты расчёта и детекторов зелёные.

**Этап 10. KPI: экран водителя и акт-документ** — ~1 день
Экран водителя «Мой расчёт» (оклад, премия, мои нарушения за месяц, итог, переключатель месяца) — строго свои данные, изоляция как у задач. Возможность приложить подписанный акт (документ-вложение) на ремонтных задачах в PWA. Прогон на телефоне (360×740), ui-guidelines.
✅ Приёмка: водитель видит свой расчёт и не видит чужой (404); на ремонтной задаче можно приложить акт; отсутствие акта и опоздание попадают в KPI как нарушение, но НЕ блокируют завершение; полный security-check зелёный.

Оценка Фазы 1.5: ~2–3 дня.

## Фаза 2 — по итогам эксплуатации (приоритизирует Артём)

Берётся в работу после приёмки фазы 1, по приоритету Артёма:

Карта на «Сегодня» (MapLibre/OSM, бесплатно для диспетчеризации); аналитика и отчёты (задачи/время по водителям, выгрузка в Excel); подпись клиента и чек-листы выездного ремонта; оптимизация порядка точек дня; S3 для фото; Telegram-дубль уведомлений; SSE вместо поллинга — только если реально нужно.

**Сделано (первый шаг «аналитики и отчётов»).** Экран «Сводка» (диспетчер/админ): задачи и время по водителям за период (день/неделя/месяц, по дате закрытия) с выгрузкой в CSV. Без миграций, без телематики и AI — решение Артёма 17–18.06.2026 (ветка `feature/driver-summary`). Следующее по приоритету в развитии телематики — due-diligence по Спутник-Мониторинг (API-ключ, состав данных, цена; согласия водителей) до любого кода интеграции.

### Процесс акта, расценки и бонус за комплектность (Фаза 2)

Решения Артёма 18.06.2026, спека — PRD §3–§5, §12.6, §13. Электронная подпись/PDF отложены — акт бумажный (водитель прикладывает фото подписанного бланка). Цель — убрать телефонную диктовку цен и ручную писанину, не давая водителю доступ к ценам. Порядок:

**Этап 11. Типы задач и требование акта на заявке** — ~1 день
Привести справочник типов к новому списку PRD §3. Переименования: «Доставка/возврат из ремонта» → «Доставка из ремонта»; «Отвезти в ТК» → «Сдача в ТК»; «Забрать СДЭК/посылку» → «Забрать посылку»; «Гарантийная замена» → «Гарантийный ремонт»; «Доставка проданного» → «Доставка проданного оборудования»; добавить «Забор с аренды». Снять обязательность фото со всех типов (фото по желанию). Аддитивная миграция: per-task флаг требования акта (+ причина снятия). Галочка-исключение «акт не нужен» в форме создания; фото снято с гейта (везде по желанию). Детектор KPI «без акта» переводится на per-task флаг и оба водителя (PRD §12.1). Стоимость гарантии — в этапе 13 (часть расценки).
✅ Приёмка: диспетчер создаёт задачи нового списка; галочка «акт не нужен» снимает требование, причина видна в карточке; DONE без фото проходит на любом типе; детектор «без акта» учитывает флаг заявки; security-check.

**Этап 12. Ведомость работ у водителя + справочник работ** — 1–2 дня
Справочник работ/запчастей (админ, частично с ценами-подсказками). Блок «Ведомость работ» в карточке водителя (выбор работ без цен + свободные строки + количество), кнопка «Отправить на расценку».
✅ Приёмка: водитель формирует ведомость и отправляет на расценку; позиции сохраняются; изоляция соблюдена; unit-тесты.

**Этап 13. Расценка у диспетчера** — ~1 день
Очередь «на расценке» с приоритетом и пуш «водитель ждёт цен»; Милена проставляет цены (подсказки из справочника), подтверждает; водитель видит готовые суммы. Стоимость гарантийного ремонта (по умолчанию бесплатно, можно указать). Жизненный цикл акта (PRD §13.4).
✅ Приёмка: после отправки водителем Милене приходит сигнал; расценка возвращается водителю; суммы видны на телефоне; security-check.

**Этап 14. Бумажный акт и видимость у диспетчера** — ~1 день
Водитель прикладывает фото подписанного бумажного акта (документ-вложение — механизм с Этапа 10 дорабатываем); диспетчер видит акт и признак комплектности в карточке и списках. Акты-описи для аренды/забора (PRD §13.2).
✅ Приёмка: акт виден диспетчеру; в карточке/списке видно, собран ли комплект; чужой документ недоступен (security-check).

**Этап 15. Бонус за комплектность** — ~1 день
Расчёт бонуса PRD §12.6 (помесячно, ≥80% актовых задач с актом, +5 000 ₽; порог/сумма в `KpiSettings`); индикаторы прогресса у водителя («Мой расчёт») и диспетчера («KPI / Зарплата»).
✅ Приёмка: бонус считается по примеру §12.6; виден обоим; 0 актовых задач → не начисляется; закрытый месяц фиксирует бонус; unit-тесты.

### Календарь загрузки и ёмкость задач (Фаза 2)

Решения Артёма 19.06.2026, спека — PRD §14, модель данных — ARCHITECTURE §4б. Подход «мост»: геокодер + расстояние по прямой × коэффициенты (петляние + пробки по времени суток). Маршрут с живыми пробками, цепочечный расчёт и оптимизация порядка точек — отложены (PRD §14.6). Поведение календаря — только подсказка/цвет, без блокировок.

**Этап 16. Движок ёмкости: оценка времени задачи** — 1–2 дня
Аддитивная миграция (ARCHITECTURE §4б): `TaskType.onSiteMinutes`; `User.specialization`; `Task.lat/lng/estimatedMinutes/estimateIsManual`; singleton `CapacitySettings` (координаты базы, рабочий день, скорость, петляние, обратная дорога); `TrafficWindow` (окна пробок). Сид: нормы по типам и окна пробок из PRD §14.2–14.3, специализация (Каширский REPAIR, Писарев/Султан DELIVERY), координаты базы. Геокодер `src/lib/geocode.ts` (внешний сервис — сверить актуальные варианты/лимиты/тарифы в вебе перед выбором; кэш по адресу; мягкий откат при сбое). Чистое ядро `src/domain/capacity.ts` (haversine, время в пути, оценка — юнит-тесты) + пересчёт оценки в `task-service` при создании и правке адреса/даты/времени/типа, если `estimateIsManual=false`. Поле «Оценка времени» в карточке задачи у диспетчера с ручной правкой и разбивкой «работа + дорога». Админ-настройки: нормы в справочнике типов, `CapacitySettings`, окна пробок, специализация водителя. security-check на все ручки.
✅ Приёмка: при создании задачи оценка считается (норма типа + дорога) и видна в карточке; ручная правка фиксируется (`estimateIsManual`) и не перезатирается пересчётом; при сбое геокодера оценка = норма работы с пометкой «дорога не учтена»; unit-тесты haversine/времени/оценки/выбора окна пробок зелёные; security-check.

**Этап 17. Вкладка «Календарь загрузки»** — 1–2 дня
Новая вкладка после «Планирования» (только диспетчер/админ, изоляция как у «Сводки»): сетка «водители × дни» на 2 недели, в ячейке «часы из рабочего дня · число задач» + статус-цвет, клик → задачи дня; оба водителя + Султан; пн–пт рабочие, сб/вс помечены нерабочими. Подсказка «кто свободен» с учётом загрузки и специализации (PRD §14.5). Опционально — индикатор загрузки на ячейках «Планирования». API `GET /api/capacity/calendar?from&to` (`capacity-service.ts`). ui-guidelines, прогон на десктопе.
✅ Приёмка: диспетчер видит загрузку обоих водителей и Султана на 2 недели; часы/число задач/цвет совпадают с оценками задач дня; перегруз подсвечивается красным, ничего не блокируется; подсказка специализации работает; security-check (ручка только для Д/А, водителю недоступна).

### Переработка механики водителя: смена и схлопывание статусов (Фаза 2)

Решение Артёма 19.06.2026 — переход от «трекинга шагов задачи» к модели «смена + одна активная задача» (как у такси). Реализовано в одной ветке (этапы тесно связаны через тесты), серией коммитов A→D. Спека — PRD §5/§5.1/§12.1, модель — ARCHITECTURE §4/§5.

**Этап A. Схлопывание и переименование статусов** — рабочая цепочка ACCEPTED/EN_ROUTE/ON_SITE → один IN_PROGRESS «В работе»; DONE «Завершена», ON_HOLD «На паузе»; до взятия у водителя статуса нет. Новая матрица; legacy-статусы оставлены в enum ради истории (правило 3); миграция открытых задач в IN_PROGRESS.
✅ Приёмка: водитель проходит «В работу → Завершить»; история не потеряна; unit-матрица и e2e зелёные.

**Этап B. Одна активная задача** — у водителя не больше одной задачи в IN_PROGRESS (сервер: `ACTIVE_TASK_EXISTS`); кнопка «В работу» блокируется проактивно. e2e гоняются последовательно (общая dev-БД + глобальное правило).
✅ Приёмка: вторую взять нельзя, пока первая не завершена; e2e.

**Этап C. Смена водителя** — модель `Shift`; открыть (водитель) → подтвердить (диспетчер) → закрыть (водитель); блок смены в «Мои задачи», блок подтверждения на доске. Изоляция как у задач.
✅ Приёмка: полный цикл смены; водитель не подтверждает (403), гость 401; security-check.

**Этап D. KPI смены и учёт времени** — метрика «поздно открыл смену» (`SHIFT_LATE`, заменяет `LATE`); порог в `CapacitySettings`; учёт отработано/простой в «Сводке» и CSV.
✅ Приёмка: поздняя смена → кандидат KPI; простой считается; unit/детектор зелёные.

### Офлайн-режим водителя (Фаза 2)

Решение Артёма 23.06.2026 — водитель должен работать без сети (на объектах связь часто пропадает), а клиент должен ощущаться как настоящее приложение. Спека — PRD §15, архитектура — ARCHITECTURE §11. Реализовано серией коммитов в одной ветке. Только клиент водителя (Android), без новых зависимостей.

**Этап O1. Серверная идемпотентность + клиентское время** — `ProcessedAction` (аддитивная миграция); `withIdempotency` (Idempotency-Key, чужой ключ → 404); `resolveOccurredAt` (clamp X-Occurred-At); 8 мутаций водителя обёрнуты; transition/comment пишут время по occurredAt. Unit + security-check.
✅ Приёмка: повтор того же действия не даёт двойного эффекта; чужой ключ → 404; время действия — момент нажатия.

**Этап O2. Кэш чтения** — IndexedDB-обёртка; `cachedFetcher` (GET кэшируется, офлайн отдаёт сохранённое); список и карточка открываются без сети; баннеры офлайна. Unit.
✅ Приёмка: офлайн список «Мои задачи» и карточка показывают последнее загруженное.

**Этап O3. Очередь записи + синхронизатор + оверлей + конфликты** — мутации офлайн в очередь, автодосылка при связи; оверлей статуса; бейджи «ждёт отправки»; конфликт (4xx при досылке) → пометка водителю. Unit + e2e.
✅ Приёмка: офлайн «В работу»/«Завершить»/комментарий/ведомость копятся и долетают при связи; статус виден сразу.

**Этап O4. Фото офлайн** — снятое без сети фото (blob в IndexedDB) досылается; индикаторы «в очереди».
✅ Приёмка: фото/акт офлайн не теряются, уходят при связи; онлайн-загрузка не сломана (photos.spec).

**Этап O5. Холодный старт без сети** — рукописный Cache API в `public/sw.js` (статика cache-first, навигация network-first), guard на localhost. Serwist не понадобился (см. ARCHITECTURE §11).
✅ Приёмка: на проде открытие приложения с иконки без сети показывает оболочку и список из кэша.

**Этап O6. Упаковка в Android APK (TWA)** — настоящее приложение (иконка, на весь экран), раздача файлом без Google Play (айфонов в парке нет). Конфиг + runbook сборки — `docs/TWA.md`.
✅ Приёмка: APK ставится на Xiaomi/Samsung, открывается как приложение, внутри — офлайн-PWA.

### 100% автономность водителя (этапы O7–O11)

Решения Артёма 02.07.2026 (план `~/.claude/plans/adaptive-pondering-penguin.md`): закрыть найденные аудитом дыры офлайна — смена online-only и врущий блок смены, вечные конфликты очереди, пустой список наутро, недоступные офлайн карточки/фото, досылка только при открытом приложении. Порядок приоритетов: утро без связи → обрыв в пути → просмотр данных.

**Этап O7. Смена офлайн + вчерашний кэш** — open/close/reopen через офлайн-очередь (kind `shift`, `/api/my/shift` под `withIdempotency`, время/день смены от момента нажатия через clamp); `Shift.openedOffline` (аддитивная миграция) + пометка «офлайн (время телефона)» Милене на доске с правом поправить время (№3); блок смены на `/m` по кэшу+оверлею (`overlayShift`), гейт «В работу» видит офлайн-открытую смену; стабильный `latest:`-ключ кэша без `date` (наутро офлайн список/смена не пустые) + бейдж «Данные за …».
✅ Приёмка: без связи смена открывается (оверлей + «не отправлено»), после связи досылается один раз с пометкой Милене; утренняя цепочка «открыл смену → взял задачу» работает офлайн; наутро офлайн виден вчерашний список с пометкой давности.

**Этап O8. Надёжность очереди** — разбор конфликтов (баннер «Не прошло — разобрать» + шторка, кнопка «Убрать» на `/m` и в карточке — решение Артёма 02.07); `overlayStatus` учитывает только pending/syncing (отклонённый переход больше не «висит»); 401/403 при досылке не превращается в вечный конфликт (стоп очереди + флаг authRequired + баннер «войдите заново», после входа досылка сама); потерянный blob фото → конфликт `BLOB_MISSING` с причиной вместо тихого успеха; `navigator.storage.persist()`; монотонный `nextSeq` + тай-брейк сортировки по id; мёртвый kind `work-item-update` убран. `runQueueOnce(deps)` вынесен для unit-тестов.
✅ Приёмка: конфликт при досылке (диспетчер отменил задачу) → баннер, «Убрать» чистит очередь, статус в UI серверный; 401 не теряет очередь; unit (runQueueOnce/overlay/seq) и e2e (конфликт-сценарий) зелёные.

**Этап O9. SW-фундамент** — имя кэша versioned из сборки (`scripts/stamp-sw-version.mjs` в `prebuild` → `public/sw-version.js`, git sha / timestamp; `sw.js` подхватывает через `importScripts`, `activate` чистит прошлые версии); фикс precache-ловушки логина (не кэшируем редиректы/`/login`; `/login` — network-only; прогрев оболочки `warm-shell` из `OfflineSync` после входа); LRU-обрезка навигаций (≤30); тестируемость (флаг `?cache=on` в URL регистрации при `NEXT_PUBLIC_SW_CACHE=on`) + e2e-профиль `pnpm e2e:sw` (прод-сборка, реальный SW, «офлайн» = остановка сервера).
✅ Приёмка: `pnpm e2e:sw` зелёный — холодный старт без сети открывает оболочку из Cache API + список из IndexedDB, логин не подменяется оболочкой; версия кэша меняется при каждой сборке (старый кэш удаляется).

**Этап O10. Просмотр всего офлайн** — префетч карточек видимого списка (`usePrefetchCards`: при связи и смене набора id кэширует `/api/tasks/:id` + справочник, затем `warm-pages` в SW прогревает HTML) → офлайн открывается любая задача дня, не только открытая вручную; офлайн-переход из списка через `<a>` вместо `<Link>` (клиентский RSC-переход без сети ненадёжен); runtime-кэш фото `/api/attachments/*` в SW (отдельный `vanmark-photos-v1`, LRU ≤100, не чистится при деплое) → просмотренные фото видны офлайн; превью офлайн-снятых фото из очереди (`PendingPhotos` — blob из IndexedDB); `/m/payroll` через `cachedFetcher` + баннер офлайна.
✅ Приёмка: `pnpm e2e:sw` — непосещённая карточка открывается офлайн (префетч+warm-pages); ранее просмотренное фото и расчёт доступны без сети; превью офлайн-снятого фото видно сразу.

**Этап O11. Background Sync** (решение Артёма 02.07) — vanilla-replay очереди в `sw.js` (обработчик `sync`, тег `vanmark-queue`: raw-IndexedDB чтение очереди, FIFO, JSON/FormData из blob, заголовки Idempotency-Key/X-Occurred-At; успех → удаление, сеть/5xx → повтор sync, 401/403 → стоп, 4xx → конфликт; `queue-replayed` в клиент). Регистрация `reg.sync.register` в `putQueued` (best-effort → деградация к тикам). Web Locks `vanmark-queue` в SW-replay и `processQueue` против гонок с открытой вкладкой (идемпотентность страхует). TTL-чистка `ProcessedAction` — `cleanupProcessedActions(60)` ночным cron 04:00 (60 дней ≫ окна 36 ч).
✅ Приёмка: офлайн-действие уходит на сервер при свёрнутом приложении (ручная проверка на телефоне — `sync` недетерминируем в Playwright); unit TTL зелёные; `offline.spec` не регрессировал (Web Lock не сломал досылку из вкладки). **Блок O7–O11 завершён — автономность водителя доведена до 100%.**

## Блок из 3 доработок (решения Артёма 03.07.2026)

Три независимые доработки по сервису водителей. Ветка A — чистый клиент; ветка B — смены глазами диспетчера (аддитивная миграция + security-check). Права уже готовы: `requireDispatcher` = ADMIN|DISPATCHER покрывает диспетчера, директора (ADMIN) и админа.

**Доработка №1. Черновик свёрнутой заявки** (клиент, без БД) — форма «Новая задача» при случайном закрытии (клик мимо/Escape/крестик) с введёнными данными сворачивается в черновик (плашка-чип внизу, несколько черновиков, `localStorage`), по клику открывается заново; «Отмена» — осознанный отказ без сохранения. PRD §8.
✅ Приёмка: заполнил форму → случайно закрыл → чип внизу → восстановил ввод; «Отмена» с переспросом выбрасывает; переживает перезагрузку. unit + e2e зелёные.

**Доработка №2. Закрытие смены диспетчером/директором/админом** — кнопка «Закрыть смену» в блоке «Смены водителей» на «Сегодня»: «Закрыть сейчас» или указать время и причину. Домен `closeShiftById` (по образцу `reopenShiftById`), `PATCH /api/shifts/:id {op:close}`, `Shift.closedById`. PRD §5.1, ARCH §4в/§7.
✅ Приёмка: Д/А закрывает смену в один клик и с ручным временем; водитель к ручке — 403; идемпотентно; security-check.

**Доработка №3. Правка времени закрытия + история смен** — правка `closedAt` задним числом (зеркало правки `openedAt`: аудит `closedAt*`, обязательная причина, запрет в закрытом месяце) + раздел **«История смен»** в «Сводке» (список смен за период с инлайн-правкой открытия/закрытия). Домен `adjustShiftClosedAt` + `getShiftHistory`, `GET /api/summary/shifts`. PRD §5.1, ARCH §4в/§7.
✅ Приёмка: правка времени открытия и закрытия из «Истории смен»; исходное время сохраняется (аудит); правка в закрытом месяце — отказ; история видна только Д/А. unit + e2e зелёные, security-check.

## Блок UI-доработок (решения Артёма 04.07.2026)

Три косметические/UX-правки существующих экранов. **Всё чисто клиентское** — ни API, ни БД, ни прав (security-check не требуется). Одна ветка.

**№1. Кнопка «Изменить» смену на «Сегодня»** — в плашке «Смены водителей» рядом с «Закрыть»/«Переоткрыть» кнопка правки времени открытия/закрытия прямо на доске (панель как в «Истории смен»), тот же `PATCH /api/shifts/:id`. Правка закрытия — только для закрытой смены.
✅ Приёмка: диспетчер правит время открытия/закрытия смены с доски; причина обязательна; e2e зелёный.

**№2. Заметные кнопки в «Истории смен»** — «Править открытие/закрытие» в «Сводке» из бледных ссылок → кнопки с рамкой и иконкой-карандашом.

**№3. Календарь загрузки** — (а) умеренно крупнее (выше строки/ячейки, крупнее шрифты и отступы); (б) окно дня переработано: полоса «Загрузка дня» + у каждой задачи индикатор статуса (зелёная галочка = завершена + подсветка, красный крест = отменена, синий кружок = в работе, серый = не начата). Статус берётся из `TaskDTO.status` — бэкенд не менялся.
✅ Приёмка: календарь читается органично; окно дня показывает статус каждой задачи. Дизайн согласован макетом. e2e зелёный.

## Блок из 2 доработок (решения Артёма 20.07.2026)

**Доработка №1. Умный поиск задач** — единое поле поиска (`TaskSearchInput`) на трёх вкладках диспетчера. «Сегодня» и «Планирование» — мгновенная клиентская фильтрация уже загруженных задач (`src/lib/task-search.ts`, zero deps): AND по словам, регистр/ё↔е, телефоны и номера по цифрам («8…»≈«+7…», «№615»), конвертация ошибочной раскладки (qwerty↔йцукен), жёлтая подсветка совпадений `<mark>`, сниппет «почему нашлось» для скрытых полей карточки, приглушение колонок без совпадений, временный разворот свёрнутых колонок с совпадениями, счётчик «Найдено N», хоткей «/», Esc/крестик. «Все задачи» — серверный `q` (расширен: +телефон по цифрам/описание/оборудование) + debounce 250 мс + подсветка.
✅ Приёмка: телефон в любом формате находит заявку на всех трёх вкладках; «fkvfpysq» находит «алмазный»; подсветка видна; счётчики честные; раскладка колонок доски не портится поиском. unit (29) + e2e (search.spec, 4 сценария) зелёные; security-check пройден ($queryRaw параметризован, права не менялись).

**Доработка №2. Напарник на задаче (двое водителей на одной точке)** *(реализация — вторым PR этой же связки, сразу за поиском)* — модель «ответственный + напарник» (crew-lead, как в field-service): `Task.coDriverId` (аддитивная миграция), статусная матрица и правило «одна активная задача» НЕ меняются. Напарник видит задачу у себя (бейдж «в паре»), шлёт фото и комментарии, статусы/ведомость — только ответственный (гейт в work-service). Пуши обоим; занятость обоим (календарь загрузки, полоса «В работе/Простой» — union интервалов); сводка: выполнено — обоим (пометка «в паре»), деньги/расценка/бонус за акты — только ответственному, суммы не удваиваются. KPI-детекторы — без изменений. При смене ответственного: на напарника → swap ролей; на третьего/снятие — напарник снимается.
✅ Приёмка: e2e co-driver.spec — видимость обоим, права напарника (фото/комментарий — да, статусы/ведомость — нет), swap, изоляция третьего водителя, сводка без удвоения, жёсткий запрет «активная парная блокирует личную задачу напарника» (до старта и после завершения — можно). Реализовано и задеплоено 20–21.07.2026 (PR #61/#62 + гейт-PR).

## Модуль «Станки» — картотека б/у оборудования (решения Артёма 05.08.2026)

Первый модуль для сотрудника вне контура «диспетчер + водители»: менеджер-сервисник Максим (должность запущена тестово 03.08.2026) ведёт парк б/у станков. Спека — PRD §16, техника — ARCHITECTURE §4г. Три этапа; сейчас делается **только первый**. Паттерн, который здесь закладывается и переиспользуется дальше (склад, цех): **новый модуль = роль + вкладка + свои таблицы + свои права**, монолит не трогаем.

**Этап 1. Картотека** — новая роль `SERVICE_MANAGER` (полный доступ к модулю у неё, у диспетчера и у админа; водителю — 404). Аддитивная миграция: `Machine` (сквозной учётный № всем станкам + «77-N» нашим), `MachineEvent` (журнал только на запись, с «было→стало»), `MachineAttachment` (фото по механике вложений задач). Домен: `machine-access.ts` (белый список ролей), `machine-status.ts` (совместимость категория×состояние, архивные исходы, `VOIDED` с причиной), `machine-service.ts`, `machine-search.ts`. API `/api/machines/*`. Экраны `/machines`: сводка-плитки с фильтрами по клику, список с умным поиском, карточка с галереей и журналом, форма создания ≤30 сек mobile-first (карточка сохраняется до фото, фото догружаются с автоповтором). Адаптив с первого экрана — Максим работает 50/50 телефон/компьютер.

**Ключевой риск этапа — не сам модуль, а ввод новой роли в существующую систему.** Обязательная часть работы: аудит ВСЕХ существующих эндпоинтов и layout'ов на допущение «роль не DRIVER ⇒ диспетчер/штаб». Права строятся белыми списками, никогда «всё кроме водителя».

✅ Приёмка: Максим заводит станок с телефона за ≤30 сек и находит его поиском по номеру/заказчику/телефону; состояние меняется, несовместимое с категорией — отклоняется; аннулирование требует причину; журнал показывает «было→стало» и кто передвинул станок; фото открывается в галерее и переживает обрыв связи при загрузке; сводка-плитки фильтруют список. **Блокер приёмки:** e2e-смоук изоляции роли в обе стороны — менеджер-сервисник получает 403/404/redirect на КАЖДОМ существующем разделе (адресно: KPI, зарплата, сводка, админка, задачи), водитель получает 404 на КАЖДОЙ ручке станков. `lint`/`typecheck`/unit/e2e зелёные, security-check пройден, прогон на 360×740. Прод: миграция enum отдельным шагом, точечный сид Максима (полный `db:seed` запрещён), фото станков попадают в существующий бэкап uploads.

**Доработки картотеки 07.08.2026 (решения Артёма, сделаны поверх этапа 1):** блок «Комментарии» в карточке (закреплённая заметка + лента, техистория свёрнута); «Задание в цех» — готовый текст из дефектовки для Telegram-группы цеха с фиксацией в журнале и переводом «В ремонте»; поле «Срок готовности/выдачи» с подсветкой «просрочен / ≤2 дней» (только подсветка, без пушей); роликовые ножи как вид оборудования в общей картотеке; форма создания — сворачиваемые доп. поля, умный подбор модели из справочника (PRD §16.8), черновик в localStorage.

**Этап 2 (не сейчас). Связь с заявками** — `Task.machineId`: заявка водителя привязана к станку, перемещения подтягиваются в картотеку сами (до этого — осознанный ручной дубль, поэтому этап приоритизирован сразу за первым). Плюс офлайн-просмотр списка станков Максиму — на площадке слабая связь.

**Этап 3 (не сейчас). Workflow ремонта** — workflow и KPI Максима, «отгрузка после оплаты», доступ цеха (Евгений/Фаха) в систему (решение Артёма 07.08: «дальше будем добавлять» — событие `shop_task` уже спроектировано под будущий экран цеха). Простое поле «Срок» перенесено в картотеку доработкой 07.08 — здесь остаются пуши/KPI по срокам. Делать, когда должность приживётся.

## Блок из 6 доработок (решения Артёма 11.08.2026)

Операционные правки по итогам работы Милены на проде + расширение должности Максима. Одна ветка, аддитивная миграция (`Task.archivedAt/archivedById`), права трогаются — security-check обязателен.

**№1. Отменённые заявки уходят с рабочих экранов** — `ListFilters.hideCancelled` + параметр `hideCancelled=1` на доске, «Планировании» и в окне дня календаря; у водителя отменённые убраны в `myTasksWhere` для обеих вкладок. Причина: отменённая заявка занимала ячейку планирования и входила в «≈ часы · N зад.», из-за чего день выглядел загруженнее, чем он есть. PRD §5, §8.
✅ Приёмка: отменённая исчезает из ячейки и из суммы часов; на доске не попадает в счётчик «Всего»; в окне дня календаря её нет; во «Все задачи» с фильтром «Отменена» — находится. e2e `task-archive.spec.ts`.

**№2. +30 минут на выгрузку всем доставкам** — нормы `TaskType.onSiteMinutes` 30→60 у трёх типов доставок (`prisma/seed-capacity.ts`, устойчиво к историческим написаниям имён). Разовый пересчёт уже заведённых заявок — `pnpm db:recompute-estimates` (только незавершённые с авто-оценкой; ручные не трогаются). В карточке появилась защита от устаревшего снимка: «Оценка посчитана по прежней норме типа» + «Пересчитать». PRD §14.2.
✅ Приёмка: новая доставка считается как 1 ч + дорога; старые незавершённые пересчитаны; ручная оценка не перезатёрта.

**№3. Кнопка «В архив» в заявке** — мягкое удаление дублей: `Task.archivedAt/archivedById`, `archiveTask`/`unarchiveTask`, `PATCH {op:archive|unarchive}`, раздел «Архив» во «Все задачи» с возвратом. Архивные исключены из ВСЕХ выборок (списки, доска, планирование, календарь, списки водителя, сводка, KPI-детекторы, пуши). Гейт закрытого месяца. PRD §5.2, ARCH §5/§7.
✅ Приёмка: заявка уходит одной кнопкой и исчезает отовсюду; находится и возвращается из «Архива»; журнал хранит обе записи; номер сохраняется; закрытый месяц отклоняется. e2e `task-archive.spec.ts`.

**№4. Права Максима: заявки — да, смены/KPI/деньги — нет** — расщепление на два белых списка (`isTaskManagerRole` vs `isDispatcherRole`), `requireTaskManager()`, вкладки по роли, флаг `canManageShifts` на доске, свой guard у `/pricing`. Попутно исправлены два места «от противного» (`transitionTask`, `loadTaskForWorksheet`) и перечисление ролей в `/api/work-catalog`. PRD §2, ARCH §6.
✅ Приёмка (**блокер**): `machines-access.spec.ts` переписан под новую границу — Максим ведёт заявки (создать/править/отменить/архивировать), но получает redirect на `/summary`, `/kpi`, `/admin` и 403 на всех ручках смен, KPI, простоя, сводки, расценки и админки; водитель по-прежнему 404 на станках. Милена и Артём работают как раньше.

**№5. Бейдж автора заявки** — `createdBy` в списочном include, маленький бейдж на карточках «Сегодня» и «Планирования», в строке «Все задачи», в окне дня календаря и строкой «Заявку создал» в карточке. В приложении водителя не показывается. PRD §4.
✅ Приёмка: видно, чья заявка (Милена/Максим), вёрстка плотной сетки планирования не разъезжается.

**№6. Умное поле времени** — `TimeField` переписан с нативного `input[type=time]` на текстовое поле с разбором (`src/lib/time-input.ts`): «1630», «16.30», «16 30», «930», «9», «сейчас». Коммит значения — сразу, как только ввод распознан (живой пересчёт длительности смены + совместимость с `fill()` в e2e). Чинит закрытие/правку смены во всех трёх местах; попутно окно времени заявки переведено на `TimeField`, а дата пометки простоя — на `DateField` (последний нативный `input[type=date]`).
✅ Приёмка: «1630» → 16:30 без нажатий; мусор откатывается; тесты смен зелёные без правок; unit `time-input.test.ts`.

## Блок доработок оборудования (решения Артёма 15.08.2026)

Семь правок по итогам эксплуатации картотеки. Меняют устройство раздела, а не отдельные экраны:
раздел «Станки» разделён на **«Листогибы»** и **«Фальцепрокатники»**, появилась комплектация и
персональный доступ вне роли. Спека — PRD §16.0, техника — ARCHITECTURE §4г/§6/§7.

1. **Сквозной системный номер убран из интерфейса.** Заголовок карточки — только учётный «77-N»
   (раньше «77-5 · №9»). Колонка `Machine.number` и последовательность остаются в БД: на них стоят
   живые записи и история. Номер убран и из поиска — искать по числу, которого человек не видит,
   значит ловить случайные совпадения.
2. **Список больше не прыгает.** Порядок серверный, по учётному номеру (был `updatedAt desc` —
   отсюда «при открытии заявки она вылетает наверх»). Над списком — выбор группировки (без групп /
   состояние / категория / вид) и направления; выбор запоминается на устройстве (localStorage).
3. **Новый вид списка.** За компьютером — плотная таблица, на телефоне — строки. Виды раздела
   переключаются плашками над списком. Счётчики сжаты в одну строку: ненулевые состояния и
   тревожные индикаторы, остальное под «Ещё».
4. **Комплектация.** `MachineKitPart`: нож к своему листогибу, размотчик и частотник к своему
   фальцепрокатнику. Смена состояния головного переспрашивает и переносит комплект (галочка
   включена); продажа списывает складские штуки один раз.
5. **Раздел «Фальцепрокатники»** с делением на фальцепрокатники, размотчики и частотники.
   Размотчики и частотники — складские остатки количеством, без состояний и категорий.
   Нумерация «77-N» своя в каждом разделе.
6. **Александр** — второй подменный водитель, полная копия Николая (вне KPI, без денежного
   профиля). Заводится безопасным прод-сидом `pnpm db:seed:roster`, пароль — `SEED_PASSWORD_ALEXANDR`.
7. **Персональный доступ к оборудованию** (`User.equipmentAccess`) для Николая и Александра.
   Роль остаётся DRIVER; флаг выдаётся в «Управление → Водители — доступ» и читается из БД, так что
   действует сразу. Флаг не открывает ничего сверх оборудования.

✅ Приёмка: у Максима и Милены в шапке два раздела; список не меняет порядок после правки карточки;
плашки видов переключают список; комплект «фальцепрокатник + размотчик» продаётся вместе и
списывает остаток ровно один раз; Николай и Александр видят только оборудование и не попадают в
задачи, смены, KPI и админку (блокер приёмки — e2e `machines-access.spec.ts`); `lint`/`typecheck`/
unit/e2e зелёные; прогон на 360×740. Прод: миграции тремя шагами (enum отдельно), затем
`pnpm db:seed:roster` — он заводит Александра и выдаёт доступ Николаю.

## Блок доработок оборудования (решения Артёма 20.08.2026)

Пятнадцать правок по итогам работы Максима на реальном парке. Спека — PRD §16 (переписаны §16.0,
§16.3–§16.6), техника — ARCHITECTURE §4г/§7. Пять миграций: «Принят» выведен из оборота, цена,
новый вид оборудования (enum — отдельным шагом), таблица скрытых подсказок моделей, категории —
массив. Задачи, смены и KPI не тронуты.

**Решения Артёма 20.08.2026:**
1. **Категорий может быть несколько** — «наш на продажу может быть и арендным». Галочками, не
   выпадашкой. «Клиентский» ни с чем не совмещается: чужое железо остаётся чужим. Схема номера
   («77-N» / «К-N») определяется наличием клиентской категории.
2. **Состояние «Принят» удалено**: станок приезжает уже с работой, карточку тут же переводили руками.
   Новые карточки заводятся в «Требует ремонта», прежние переведены туда же миграцией.
3. **Диагностика и сверка — обязательные операции**, а не индикаторы по календарю: янтарный баннер
   сверху карточки, как открытие смены у водителя. Отметка делается один раз — при заведении и
   заново после возврата из аренды (сервер сбрасывает отметки сам и пишет об этом в журнал).
4. **Цена** (целые рубли, одна на продажу и аренду) и **толщина металла** — на видное место в форме
   и отдельными колонками в списке.
5. **Фальц машинки** — третий вид в разделе «Листогибы», штучный, как ножи (не складской остаток).
6. **Никаких выпадающих списков**: фильтры переехали в боковую панель строками с галочкой, фильтр по
   состоянию убран совсем (его роль выполняют счётчики), «Ответственный» и «Вид» в формах — тоже
   строками. Правило записано в скилл `ui-guidelines`.
7. **Счётчики над списком**: всегда «Всего / Наш на продажу / Наш арендный / Клиентский / В аренде»,
   остальное под «Ещё». Станок с двумя категориями считается в обеих — сумма по категориям может
   быть больше «Всего», и это правда, а не ошибка.
8. **Ширину колонок можно тянуть** (двойной клик — сброс), раскладка своя у каждого раздела и
   помнится на устройстве. Колонки: Номер · Фото · Модель · Состояние · Цена · Толщина · Срок.
9. **Фото листаются** в полноэкранном просмотре — и в карточке станка, и у водителя в задаче.
10. **Комплектация галочками** (листогиб: нож / машинка / стойка; фальцепрокатник: размотчик /
    частотник) плюс «свой вариант»; в базе остаётся одной строкой.
11. **Подсказку модели можно скрыть крестиком** — в карточки заносят временные названия. Данные не
    меняются, заведение карточки с тем же именем возвращает подсказку.
12. **Убраны из интерфейса** серийный номер, место на площадке, заказчик и телефон (колонки в БД
    целы), а также индикатор «Без заказа 1С». Кнопка «Редактировать» вынесена в шапку карточки.

✅ Приёмка: обе вкладки работают одинаково; счётчики стоят в порядке Артёма; фильтры без выпадашек;
у новой карточки виден баннер отметок и гаснет после двух нажатий, а после возврата из аренды
появляется снова; станок с двумя категориями получает и «Продан», и «В аренде»; ширина колонок
тянется и переживает перезагрузку; `lint`/`typecheck`/unit/e2e зелёные; прогон на 360×740.
Прод: пять миграций подряд (`migrate deploy`), enum-шаг отдельной миграцией; бэкап перед накаткой —
миграция категорий переносит данные и сносит старую колонку.

## Эксклюзивные категории + связь станков с заявками (решения Артёма 21.08.2026)

Две доработки. Первая маленькая, вторая закрывает **этап 2 модуля оборудования** (PRD §16.1) —
тот самый ручной дубль «водитель отвёз, а карточку переводит кто-то отдельно». Спека — PRD §4,
§16.1, §16.3, §16.9; техника — ARCHITECTURE §4г, §6, §7. Три миграции (значения enum отдельным
шагом, `TaskType.machineFlow` с бэкфиллом по именам, таблица `TaskMachine`). Статусная матрица
задач и правила смен/KPI не тронуты ни на строку.

**Решения Артёма 21.08.2026:**
1. **Две новые категории оборудования** — «Выставочный вариант» и «Под настройку ножей». Обе
   **эксклюзивные**: не совмещаются ни с чем, как «Клиентский». Это наше железо, номер «77-N».
   Продать или сдать их можно кнопкой прямо из карточки — эксклюзивная категория при этом
   **заменяется** на «Наш на продажу» / «Наш арендный», и переезд пишется в журнал.
2. **Станок цепляется к заявке** прямо из формы («везём станок на продажу → подцепляем к заявке»).
   Станков может быть несколько, обычно один; комплект головного едет вместе с ним **всегда**.
3. **При завершении заявки станок сам меняет состояние** по правилу типа (таблица — PRD §16.9), а в
   его журнал пишется «… по заявке №615». Ошибки автоматики **никогда не блокируют водителя**:
   что не получилось — заметкой в журнал, диспетчер дожмёт руками.
4. **Правило задаётся типом в `/admin/task-types`**, колонкой «Автоматика по станку» — Артём заводит
   новые типы без программиста. Дефолт «Ничего не делать» безопасен.
5. **Направление** («везём клиенту / забираем к нам») спрашивается только у двунаправленных типов,
   сегментом из двух кнопок с предвыбором по состоянию станка. Никаких выпадашек.
6. **Новый станок заводится прямо из формы заявки** — «привезли на выкуп, карточки ещё нет».
7. **Водителю показываем всё, кроме цены**: номер, модель, состав комплекта, фото станка.
8. **Забор с аренды возвращает станок в «Готов»**, отметки диагностики и сверки нужны заново.

✅ Приёмка: Милена заводит «Доставку проданного», цепляет станок из пикера (или заводит новый
оттуда же); Каширский видит в телефоне блок «Станок» с комплектом и фото и НЕ видит цены;
завершение переводит станок в «Продан», комплект списывается, журнал полный; забор с аренды
возвращает станок в «Готов» с горящим баннером отметок; откат и повторное завершение не продают
станок дважды; выставочный станок эксклюзивен по категории и продаётся кнопкой с переездом
категории; `lint`/`typecheck`/unit/e2e зелёные; прогон на 360×740.
Прод: три миграции подряд (`migrate deploy`), enum-шаг отдельной миграцией. **Задеплоено 21.08.2026**
(`3ae73e2`): справочник типов на проде совпал с сидом один в один, поэтому бэкфилл проставил правило
всем восьми типам сам — руками ничего доводить не пришлось. Правило нужно выбирать только у типов,
которые Артём заведёт позже: у нового типа оно по умолчанию «Ничего не делать».

## Вкладка «Команда» — дни рождения и отпуска (решения Артёма 18.08.2026)

Справочник коллектива VanMark: кто у нас работает, у кого когда день рождения, кто когда в отпуске —
плюс автоматическая рассылка о днях рождения. Спека — PRD §18, техника — ARCHITECTURE §4д/§6/§7/§8.
Аддитивно: две миграции (значение enum — отдельным шагом), статусная матрица, задачи и KPI не трогаются.

**Решения Артёма 18.08.2026:**
1. Справочник охватывает **весь** коллектив, включая сотрудников БЕЗ доступа в систему (цех и др.) —
   иначе половина именинников осталась бы за бортом.
2. Вкладку видят офисные роли (диспетчер, админ, менеджер-сервисник), правят — только диспетчер и
   админ: Максим смотрит. Водителям раздела нет (у них своё приложение), но пуши о ДР они получают.
3. Рассылка — **за 3 дня и в сам день**, всем действующим сотрудникам со входом, кроме именинника;
   имениннику в его день — личное поздравление. Время **09:00 МСК**, отдельно от утреннего пуша
   водителям в 08:00 — иначе два уведомления слипнутся.
4. Отпуска всего коллектива — в **существующем** реестре отсутствий (те же записи, что видит
   «Календарь загрузки»), а не во второй сущности ради тех же дат.

**Что сделано.** Роль `EMPLOYEE` — сотрудник без доступа (`canLogin=false`, технический логин
`emp-<uuid>`, случайный пароль); роль не входит НИ В ОДИН белый список и не открывает ничего.
Поле `User.birthday` (`@db.Date`, опционально): год храним, показываем без года. Чистое ядро
`src/domain/birthdays.ts` (29.02 → 28.02 в невисокосный год, ближайшая дата через Новый год) с
юнит-тестами и `src/domain/team-service.ts` (снимок экрана, заведение, правка, мягкая деактивация).
API `/api/team`: чтение — `requireTaskManager`, вся запись — `requireDispatcher`; роль, логин и
доступы из тела не принимаются вовсе. Отпуска — через существующие `/api/absences` с ослабленной
валидацией («любой действующий внутренний сотрудник» вместо «роль DRIVER»). Три билдера пушей +
`runBirthdayReminders` в cron 09:00. Кнопка «Включить уведомления» показана и менеджеру-сервиснику —
раньше она пряталась, потому что ему не слали ни одного пуша.

✅ Приёмка: Милена заводит цехового сотрудника без входа, ставит ему день рождения и отпуск; попытка
сменить имя у учётки С доступом отклоняется («здесь меняется только день рождения»); Максим вкладку
видит, но не правит (запись — 403), водитель не попадает ни на `/team`, ни на `/api/team`; под
заведённым сотрудником войти нельзя (вход запрещён, пароль случайный, админская ручка доступа его не
видит — 404); отпуск не водителя не сдвигает «Календарь загрузки» и KPI; за 3 дня и в сам день пуш
приходит всем со входом, кроме именинника, а имениннику — поздравление; 29 февраля в невисокосный год
празднуется 28-го. `lint`/`typecheck` чистые, unit зелёные (`birthdays.test.ts`, `notifications.test.ts`,
`roles.test.ts`), e2e — `e2e/team.spec.ts` (сценарий Милены целиком, права трёх ролей, инвариант
«вход не включить»), security-check пройден, прогон на 360×740 (Максим работает с телефона). Прод:
миграции двумя шагами (enum отдельно), процесс приложения по-прежнему ОДИН — иначе поздравления
задвоятся.

## Блок из 3 доработок (решения Артёма 22.08.2026)

Три просьбы одним блоком: заводить заявки на основе старых, переработать «Управление» (стартовый
экран админа — «неудобно и многих функций не хватает») и сделать читаемой «Сводку». Миграций нет,
новых ручек нет — только один аддитивный ответ (`workdayMinutes` в overview Сводки).

1. **Копирование задачи** (PRD §8 п.5, §17.3). Кнопка «Копировать» в карточке (обе панели
   «Действия») и в строке «Все задачи», для обоих контуров. Копия — обычная новая задача через
   существующий `POST /api/tasks`: форма создания, предзаполненная из исходной. Не наследуются дата
   (ставится сегодня), исполнитель, напарник, стоимость внешнего перевозчика и всё серверное
   (статус, история, фото, акты, ведомость, факт оплаты); оплата копируется — поэтому под
   заголовком янтарная подсказка «проверьте дату, исполнителя и оплату». Направление станков
   пересчитывается по их текущему состоянию, выключенный тип заменяется первым активным.
   `formFromTask` переехал из модалки в чистый `lib/task-draft` (+ `formForCopy`, `copyTitle`,
   `copyHint`), `pickerLabel` — в `lib/machine-ui`. Попутно: карточка получила режим формы
   (edit|copy|draft) с `key` по режиму — форма правки больше не показывает устаревшие поля после
   `mutate()`; в сегменте «Цех» кнопка «Задача» открывает форму цеха, а не доставки.

2. **Сводка v3** (PRD §8 п.7). Итоги за период плитками с нейтральным «было N», таблица-сравнение
   водителей вместо карточек, раскрытие подробностей по клику на цифру (график по дням с
   фиксированной шкалой = рабочий день + список), период в адресе `?g&d` (сервер читает
   `searchParams`, клиент правит адрес `history.replaceState`), `keepPreviousData` вместо мигающего
   «Загрузка…». Общие примитивы: `StatTile`, `LoadBar`, `Segmented` (`button aria-pressed`, не
   `radio`), `formatDuration` — три разных формата одной длительности сведены к одному.

3. **«Управление» v2** (PRD §8 п.10–11). Навигация в `(admin)/layout` (экран перестал быть
   тупиком), блок «Требует внимания» из уже существующих источников через `Promise.allSettled`,
   все разделы группами, блок «Система» (версия сборки, пуш-подписки, расписание из чистого
   `lib/cron-schedule.ts`, ссылка на `/api/health`). Учётки офиса: пароль и вход теперь меняются
   Милене, Максиму и Михаилу — ручки работают с белым списком `MANAGED_ROLES`, EMPLOYEE → 404,
   водительские признаки остались только у DRIVER; себе вход не закрыть, последнего админа со
   входом не выключить (`LAST_ADMIN`). Экран переименован в «Пользователи и доступ». Попутно
   `/kpi?period=`, `/machines?flag=`, `/seamers?flag=` читают адрес, а `/board#attention` доводит
   до блока после загрузки данных.

✅ Приёмка: Милена копирует заявку с карточки и из списка — новая заявка с сегодняшней датой, без
исполнителя, с другим номером; копия задачи цеха получает свой «Ц-N»; Артём открывает «Управление»
с вкладками, видит «Требует внимания» и меняет пароль Максиму; выключить вход себе и последнему
админу не даёт; Сводка читается одной таблицей, период переживает перезагрузку и делится ссылкой.
`lint`/`typecheck` чистые, unit зелёные (`task-draft`, `summary-url`, `format-duration`,
`admin-overview`, `users`), e2e — `task-copy`, `summary-v3`, `admin-home`, `admin-drivers` плюс
обновлённый `summary-v2`; security-check пройден (новые права: ADMIN-only, личность из сессии,
404 для чужих ролей, self-lockout и последний админ, пароли argon2id и не в ответе).

## Отложено: 1С

Интеграция с 1С:УНФ снята из плана (продуктовое решение Артёма от 04.06.2026) — сервис работает полностью автономно. Наработки сохранены как задел в `docs/archive/1C-INTEGRATION.md` (режимы обмена, маппинг, надёжность) и скилл `docs/archive/skills/1c-sync/`; поле `Task.externalId1C` из схемы убрано. Если интеграцию решат вернуть — это отдельная фаза: восстановить документ и скилл из архива, добавить `externalId1C` аддитивной миграцией. До явного решения Артёма тему не трогаем.

## Риски

| Риск | Митигация |
|---|---|
| Команда продолжит ставить задачи в чате по привычке | Пилот с жёстким правилом «задача = сервис», Милена — главный союзник; форма создания ≤30 сек |
| Фото съедят диск VPS | Сжатие на клиенте, мониторинг места в админке, S3 в фазе 2 |
| Слабый интернет у водителя на объекте | Оптимистичный UI + ретраи; критические действия не теряются |
| Расползание скоупа («давай ещё CRM/склад/маршруты») | Roadmap — закон; новые идеи — в бэклог фазы 2 |

## Что нужно от Артёма по ходу

Старт нумерации задач решён — с 476 (предварительно, сверить с чатом перед прод-запуском). К этапу 7: купить VPS и домен (или поддомен van-mark.ru, например drive.van-mark.ru). К этапу 8: договориться с Миленой и водителями о правилах пилота. К Фазе 1.5 (KPI, этап 9): точные суммы — оклад каждого водителя, размер премии, веса штрафов (опоздание / без акта / невыполненная точка), шаг прогрессии (до этого работаем на дефолтах-заглушках).

К Календарю загрузки (Этапы 16–17): все вводные получены и подтверждены Артёмом 19.06.2026 — база и нормы по типам (PRD §14.1–14.2), коэффициенты пробок и петляния (§14.3), средняя скорость 50 км/ч, нормы «Забор с аренды» 40 мин и «Прочее» 30 мин. Все параметры правятся в админке и калибруются по факту.
